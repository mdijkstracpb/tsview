# View multiple time series in browser

To use the package, paste the following in R.
  
	install.package("devtools")
  
	devtools::install_github("mdijkstracpb/tsview")
	
And type `tsview(gui = T)` for a demo.